<?xml version="1.0" encoding="utf-8"?>
<!-- Network security config:
     - cleartextTrafficPermitted=true para domains de dev local (LAN IPs).
     - Em prod, dominios reais usam HTTPS automatico (cleartext bloqueado por padrao).
     - localhost / 10.0.2.2 (alias Android emulador para localhost do host) liberados. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <!-- Faixas de IP privadas comuns para desenvolvimento via LAN -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">192.168.1.11</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
